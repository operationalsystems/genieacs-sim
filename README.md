# genieacs-sim

TR-069 client simulator for GenieACS.

To install:

    git clone https://github.com/operationalsystems/genieacs-sim
    cd genieacs-sim
    npm install

To use:

    ./genieacs-sim --help
